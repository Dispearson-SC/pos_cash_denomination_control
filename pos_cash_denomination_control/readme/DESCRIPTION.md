Adds denomination breakdown, cash-move reasons, vault withdrawal alerts, and
a closing-manager override to Point of Sale cash control.

**Behavior change on install**: cash-in operations are disabled by default
for every Point of Sale, including every point of sale configuration that
already existed before this module was installed. See `CONFIGURE.md` to
re-enable cash-in per point of sale.
